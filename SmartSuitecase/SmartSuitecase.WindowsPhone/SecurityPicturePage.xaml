<Page
    x:Class="SmartSuitecase.SecurityPicturePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartSuitecase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Azure">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBox Text="{Binding}" FontFamily="Segoe UI" Background="Azure" Foreground="#00695C"  FontWeight="Bold" FontSize="38" Margin="0,30,0,0" BorderBrush="{x:Null}"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Text="{Binding}" FontFamily="Segoe UI" Background="Azure" Foreground="#00695C"  FontWeight="Bold" FontSize="20" BorderBrush="{x:Null}"/>
                        <TextBox Text="{Binding}" FontFamily="Segoe UI" Background="Azure" Foreground="#00695C"  FontWeight="Bold" FontSize="16" BorderBrush="{x:Null}"/>
                    </StackPanel>
                </StackPanel>
            <Image Source="{Binding}" Margin="10"></Image>
        </StackPanel>
    </Grid>
</Page>
